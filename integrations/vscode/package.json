{
  "name": "fettle",
  "displayName": "Fettle",
  "description": "Python diagnostics from Fettle's AI-assisted development quality policy",
  "version": "0.9.1",
  "publisher": "MilindGaharwar",
  "icon": "icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MilindGaharwar/fettle"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Linters",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "lint",
    "quality",
    "python",
    "ruff",
    "semgrep",
    "ai",
    "claude"
  ],
  "activationEvents": [
    "onLanguage:python"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Fettle",
      "properties": {
        "fettle.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable Fettle diagnostics"
        },
        "fettle.pluginPath": {
          "type": "string",
          "default": "",
          "description": "Path to Fettle plugin root (auto-detected from ~/.claude/plugins/fettle)"
        },
        "fettle.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Path to Python >= 3.11 interpreter (auto-detected)"
        }
      }
    },
    "commands": [
      {
        "command": "fettle.restart",
        "title": "Fettle: Restart Language Server"
      },
      {
        "command": "fettle.runQualityScan",
        "title": "Fettle: Run Quality Scan"
      },
      {
        "command": "fettle.showReport",
        "title": "Fettle: Show Effectiveness Report"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vscode-languageclient": "^9.0.1"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  }
}
