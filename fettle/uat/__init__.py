"""Agentic UAT — WP3 (Stage 5; design doc 10).

Independent acceptance layer: active specs are the contract, sessions
explore the running product the way a user would, observations reconcile
to per-scenario verdicts. This package is CLI-world (minutes), never
dispatcher-world (milliseconds).
"""
