---
description: "Run full Fettle quality scan on the current project"
---
<!-- generated by fettle workflows install -->

$ARGUMENTS refers to any text the user typed after the slash command.

Run `fettle check --all` (pass any additional arguments from $ARGUMENTS).

Present the results to the user, grouped by severity. If there are errors, suggest specific fixes based on the rule messages. If a baseline is available, highlight only new findings.

After presenting results, offer to:
1. Fix the errors automatically (if any) with `fettle check --fix`
2. Update the baseline with `fettle baseline update`
3. Run a cross-review with `python3 -m fettle.cross_review`
