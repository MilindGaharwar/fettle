# Fettle self-scan ignore list — findings in these paths are excluded.
# tests/fixtures holds intentional rule violations used as test corpora.
tests/fixtures/*
# Deliberately fails lint so the Assurance example can demonstrate detection.
examples/assurance-loop/broken.py
