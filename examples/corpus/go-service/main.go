// Demo CLI surface: prints ledger balances as text. Intentionally minimal;
// Fettle's import provider currently declares Go coverage out of scope.
package main

import "fmt"

func main() {
	fmt.Println("ledger-cli: no balances configured")
}
