[project]
name = "ledger-demo"
version = "0.1.0"
description = "Fettle demo corpus: minimal double-entry ledger service"
requires-python = ">=3.11"

[tool.fettle]
# Marker only: makes this directory a detectable Python workspace.
