SPECIFICATION Spec

CONSTANTS
    Items = {item_a, item_b}
    Sessions = {sess_1, sess_2}
    Worktrees = {wt_1, wt_2}
    Files = {file_x, file_y}

\* Safety invariants (checked in every reachable state)
INVARIANTS
    NoDuplicateClaim
    UnknownScopeConservative
    ClaimBeforeWork
    LockMutualExclusion
